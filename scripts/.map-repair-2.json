{"edits":[{"path":"App/map-roll-transition.js","before":"6b4ef0c2f65d268119512cb4a7222f5f900fe27f19d5dc14d3a8469ab5e7be3d","after":"e4d59fc3aba1727f7dd1305cb5c707d4b3ef8f53e7fbc1d7bd177c0318a83502","hunks":[[2,3,""],[5,200,"  if (root?.document) api.install(root);\n"],[203,203,"  // Lateral play and blur provide the settling motion. Never pull a sheet past\n  // its registered edge: vertical overshoot exposes the empty map aperture.\n"],[205,206,"    [56, 61], [73, 82], [88, 99], [93, 100], [97, 100], [100, 100]\n"],[207,208,"  const MOTION_CLASSES = [\"map-roll-to-surface\", \"map-roll-to-regional\", \"map-roll-demo-out\", \"map-roll-demo-in\"];\n\n  function install(root) {\n    const doc = root.document;\n    const shell = doc.getElementById(\"route-map-shell\");\n    if (!shell || shell.dadRadarMapRoll) return shell?.dadRadarMapRoll ?? null;\n    const audio = root.dadRadarMapRollAudio?.createController?.({volume: 0.72}) ?? null;\n    const frame = callback => root.requestAnimationFrame(callback);\n    const reduced = () => root.matchMedia?.(\"(prefers-reduced-motion: reduce)\").matches === true;\n    let surface = null, observer = null, moving = false, registering = false;\n    let current = false, queued = null, target = false, timer = null, generation = 0;\n    let lastToken = null, demoReturn = null;\n    let pendingAnimations = new Map();\n\n    root.addEventListener(\"pointerdown\", () => { void audio?.unlock?.(); }, {once: true});\n    if (!doc.querySelector(\"link[data-dad-radar-map-roll]\")) {\n      const link = doc.createElement(\"link\");\n      link.rel = \"stylesheet\";\n      link.href = \"/UI/map-roll-transition.css?v=7\";\n      link.dataset.dadRadarMapRoll = \"true\";\n      doc.head.appendChild(link);\n    }\n    [\"top\", \"bottom\"].forEach(edge => {\n      if (shell.querySelector(`.map-roll-edge-shadow.is-${edge}`)) return;\n      const shadow = doc.createElement(\"div\");\n      shadow.className = `map-roll-edge-shadow is-${edge}`;\n      shadow.setAttribute(\"aria-hidden\", \"true\");\n      shell.appendChild(shadow);\n    });\n    let splice = shell.querySelector(\".map-roll-splice\");\n    if (!splice) {\n      splice = doc.createElement(\"div\");\n      splice.className = \"map-roll-splice\";\n      splice.setAttribute(\"aria-hidden\", \"true\");\n      shell.appendChild(splice);\n    }\n\n    // Measure the aperture, not the viewport. Full-family has a smaller map\n    // within the same window; viewport breakpoints made its housings collide.\n    let lastHardwareWidth = 0;\n    function fitHardware() {\n      const width = shell.clientWidth;\n      if (!width || width === lastHardwareWidth) return;\n      lastHardwareWidth = width;\n      const scale = Math.min(1, width / 650);\n      const sizes = {\"clock-width\":184, \"sequence-width\":196, \"side-inset\":18,\n        \"clock-bottom\":18, \"sequence-bottom\":14, \"font\":20, \"label\":9,\n        \"zone\":8, \"pad-x\":10, \"pad-y\":7, \"arm-height\":18, \"bracket-width\":34};\n      Object.keys(sizes).forEach(name => shell.style.setProperty(`--map-hardware-${name}`, `${sizes[name] * scale}px`));\n      shell.style.setProperty(\"--map-hardware-spacing\", `${0.6 * scale}px`);\n    }\n    fitHardware();\n    root.addEventListener(\"resize\", fitHardware);\n    if (typeof root.ResizeObserver === \"function\") new root.ResizeObserver(fitHardware).observe(shell);\n    else new root.MutationObserver(fitHardware).observe(doc.getElementById(\"dashboard\") || shell,\n      {attributes: true, attributeFilter: [\"hidden\", \"style\"]});\n\n    function observe() {\n      if (observer && surface) observer.observe(surface, {attributes: true, attributeFilter: [\"hidden\"]});\n    }\n    function setHidden(hidden) {\n      if (!surface || surface.hidden === hidden) return;\n      // Disconnect only for our synchronous write. A timer-based suppression\n      // window used to swallow legitimate live updates in the same frame.\n      observer?.disconnect();\n      surface.hidden = hidden;\n      observe();\n    }\n    function clearMotion() { shell.classList.remove(...MOTION_CLASSES); }\n    function clearWatch() {\n      if (timer !== null) root.clearTimeout(timer);\n      timer = null;\n      shell.removeEventListener(\"animationend\", onAnimationEnd);\n      shell.removeEventListener(\"animationcancel\", onAnimationCancel);\n      pendingAnimations.clear();\n    }\n    function registered() {\n      shell.classList.toggle(\"is-surface-registered\", current);\n      setHidden(!current);\n      clearMotion();\n    }\n    function drain() {\n      registering = false;\n      // Read the most recent request NOW, not one captured before the clacks.\n      const next = queued;\n      queued = null;\n      if (next !== null && next !== current) transitionTo(next);\n      else if (demoReturn !== null) {\n        const returnTo = demoReturn;\n        demoReturn = null;\n        transitionTo(returnTo);\n      } else if (!current && surface?.classList.contains(\"map-roll-demo-sheet\")) {\n        observer?.disconnect();\n        surface.remove();\n        surface = null;\n      }\n    }\n    function finish(withSound = true) {\n      if (!moving) return;\n      clearWatch();\n      current = target;\n      registered();\n      moving = false;\n      registering = true;\n      audio?.stopMotor?.();\n      const token = generation;\n      // Two animation frames ensure the final registered style has had a paint\n      // opportunity. A canceled/new transport invalidates both delayed sounds.\n      frame(() => frame(() => {\n        if (token !== generation || moving) return;\n        if (withSound && !reduced()) void audio?.playRegisterClack?.();\n        root.setTimeout(() => {\n          if (token !== generation || moving) return;\n          if (withSound && !reduced()) void audio?.playDetentClack?.();\n          root.setTimeout(() => { if (token === generation) drain(); }, 85);\n        }, withSound && !reduced() ? 135 : 0);\n      }));\n    }\n    function onAnimationEnd(event) {\n      if (pendingAnimations.get(event.target) !== event.animationName) return;\n      pendingAnimations.delete(event.target);\n      if (!pendingAnimations.size) finish();\n    }\n    function onAnimationCancel(event) {\n      if (pendingAnimations.get(event.target) === event.animationName) finish(false);\n    }\n    function transitionTo(next) {\n      if (!surface) return;\n      if (moving || registering) {\n        queued = next;\n        setHidden(moving ? false : !current);\n        return;\n      }\n      if (next === current) { registered(); return; }\n      generation += 1;\n      audio?.cancelRegistration?.();\n      moving = true;\n      target = next;\n      clearMotion();\n      // Put BOTH sheets into their start positions in the same task as showing\n      // the incoming sheet. No intermediate untransformed sheet gets painted.\n      shell.classList.remove(\"is-surface-registered\");\n      shell.classList.add(next ? \"map-roll-to-surface\" : \"map-roll-to-regional\");\n      setHidden(false);\n      const map = shell.querySelector(\".route-map-svg\");\n      pendingAnimations = new Map([[map, next ? \"map-roll-regional-up\" : \"map-roll-regional-down\"],\n        [surface, next ? \"map-roll-surface-up\" : \"map-roll-surface-down\"],\n        [splice, next ? \"map-roll-splice-up\" : \"map-roll-splice-down\"]]);\n      shell.addEventListener(\"animationend\", onAnimationEnd);\n      shell.addEventListener(\"animationcancel\", onAnimationCancel);\n      void shell.offsetHeight;\n      if (!reduced()) void audio?.playMotor?.();\n      // A hidden tab or disabled stylesheet may not deliver animationend.\n      timer = root.setTimeout(() => finish(false), (reduced() ? 1 : DURATION_MS) + 500);\n    }\n    function attach(layer) {\n      if (!layer || layer === surface) return;\n      const requested = !layer.hidden;\n      const old = surface;\n      generation += 1;\n      clearWatch();\n      observer?.disconnect();\n      audio?.stopMotor?.();\n      audio?.cancelRegistration?.();\n      clearMotion();\n      moving = registering = false;\n      queued = null;\n      current = false;\n      surface = layer;\n      if (!layer.classList.contains(\"map-roll-demo-sheet\")) demoReturn = null;\n      shell.classList.remove(\"is-surface-registered\");\n      if (old?.classList.contains(\"map-roll-demo-sheet\")) old.remove();\n      observer = new root.MutationObserver(() => {\n        const requestedSurface = !surface.hidden;\n        transitionTo(requestedSurface);\n      });\n      observe();\n      setHidden(true);\n      if (requested) transitionTo(true);\n    }\n    function discover() {\n      attach(shell.querySelector(\".airport-surface-layer:not(.map-roll-demo-sheet)\"));\n    }\n    new root.MutationObserver(discover).observe(shell, {childList: true});\n    discover();\n\n    function testRoll() {\n      if (moving || registering) return;\n      if (!surface) {\n        const map = shell.querySelector(\".route-map-svg\");\n        if (!map) return;\n        // No airport geometry available: roll a second copy of the existing\n        // map, not an empty aperture or a fabricated airport chart.\n        const sheet = doc.createElement(\"div\");\n        sheet.className = \"airport-surface-layer map-roll-demo-sheet\";\n        sheet.hidden = true;\n        sheet.setAttribute(\"aria-hidden\", \"true\");\n        const copy = map.cloneNode(true);\n        copy.removeAttribute(\"id\");\n        copy.setAttribute(\"class\", \"map-roll-preview-svg\");\n        copy.querySelectorAll(\"[id]\").forEach(node => node.removeAttribute(\"id\"));\n        sheet.appendChild(copy);\n        shell.appendChild(sheet);\n        attach(sheet);\n      }\n      demoReturn = current;\n      transitionTo(!current);\n    }\n    root.addEventListener(\"dad-radar:visual-state-change\", event => {\n      const token = event.detail?.state?.diagnostics?.shutterTestToken ?? null;\n      if (!token || token === lastToken) return;\n      lastToken = token;\n      testRoll();\n    }, true);\n    function requestSurface(layer, visible) {\n      attach(layer);\n      transitionTo(Boolean(visible));\n    }\n    shell.dadRadarMapRoll = {testRoll, fitHardware, requestSurface};\n    return shell.dadRadarMapRoll;\n  }\n  return {DURATION_MS, PROFILE, install};\n"]]}]}