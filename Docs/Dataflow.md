Calendar service arrow state engine, where trips and key times become state changes. Flight data service arrow state engine, where live status refines the current state.

State engine arrow UI, where screens and animations react to high-level states.
